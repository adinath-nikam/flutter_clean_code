name: flutter_clean_code
description: A starting point for Dart libraries or applications.
version: 1.0.0
homepage: https://github.com/adinath-nikam/flutter_clean_code
repository: https://github.com/adinath-nikam/flutter_clean_code

environment:
  sdk: ^3.4.4

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

  # Explicitly Added
  analyzer: ^5.0.0
  custom_lint_builder: ^0.5.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
